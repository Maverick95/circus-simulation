#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include "stdafx.h"

namespace Functions
{
	unsigned int LowestCommonMultiple(unsigned int *, const unsigned int &);
	unsigned int Bits(unsigned int);
}

#endif
